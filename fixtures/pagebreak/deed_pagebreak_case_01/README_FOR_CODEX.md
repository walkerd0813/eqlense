# Codex Fixture: Pagebreak

Files:
- sample.pdf (3 pages: fixture page_index 0..2 corresponding to original pages 99..101)
- expected.json (gold truth - review/update after running)
- actual.json (generated by helper script)
- notes.md (context)

Quick workflow:
1) Run the pipeline (PowerShell runner) against the fixture:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\seller-app\backend\scripts\_registry\hampden\Run-RecordedLand-Pipeline-All-v1_1_PS51SAFE.ps1" \
	-Root "C:\seller-app\backend" \
	-Pdf  "C:\seller-app\backend\fixtures\pagebreak\deed_pagebreak_case_01\sample.pdf" \
	-DocType "DEED" \
	-StartPage 0 \
	-EndPageExclusive 3 \
	-ChunkSize 50
```

2) Generate a compact actual JSON for diffing:

```powershell
c:\python314\python.exe "c:\seller-app\backend\scripts\_registry\hampden\generate_fixture_actual_from_join_v1.py" \
	--join "<work_folder>\p00000_p00002\join__DEED__p00000_p00002__v1_3_1.ndjson" \
	--out "C:\seller-app\backend\fixtures\pagebreak\deed_pagebreak_case_01\actual.json"
```

3) Diff `actual.json` vs `expected.json` and update `expected.json` with human-verified gold truth.

Notes about the stitcher patch applied:
- The stitcher now supports scanning a "page-prefix" (top-of-next-page) without requiring a preceding transaction boundary. This prevents the extractor from skipping valid continuation lines when callers already limited the scan to the safe prefix region.
- The change is conservative: default behavior still requires a boundary unless the caller explicitly requests prefix-mode.

If you want me to update `expected.json` with the exact `actual.json` contents or to run a human-verified diff, tell me and I'll proceed.
