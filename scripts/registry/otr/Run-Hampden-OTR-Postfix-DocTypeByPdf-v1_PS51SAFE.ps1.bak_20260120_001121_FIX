param(
  [Parameter(Mandatory=\True)][string]\,
  [Parameter(Mandatory=\True)][string]\
)
\Stop = 'Stop'

if (-not (Test-Path \)) { throw "InEvents not found: \" }
New-Item -ItemType Directory -Force -Path \ | Out-Null

Write-Host "[run] postfix doc_type by PDF -> \"
python (Join-Path (Split-Path \C:\seller-app\backend\scripts\registry\otr -Parent) "otr\otr_postfix_doctype_by_pdf_v1.py") --in_events "\" --out_dir "\"